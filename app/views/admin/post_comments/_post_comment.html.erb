<table class='table'>
 <tbody>
 <% post.post_comments.each do |post_comment| %>
   <tr>
     <td><%= image_tag post_comment.user.get_profile_image, size:"50x50" %>
     </td>
     <td><%= post_comment.comment %></td>
     <td><%= link_to "削除", admin_post_post_comment_path(post, post_comment), method: :delete, class: "btn btn-sm btn-danger", remote: true %></td>
   </tr>
 <% end %>
 </tbody>
</table>